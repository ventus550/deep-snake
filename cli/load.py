from utils import initialize

print(initialize("demo").__dict__)
# agent.load("net")
# print(json.dumps(data, indent=3, sort_keys=True))

# pprint(agent.__dict__)

# t = torch.tensor((
# 	(0, 0, 1),
# 	(0, 1, 1),
# 	(0, 0, 0),
# ), dtype=torch.float)
# print(agent(t), t)


# agent.train(env = env, live = False, scheduler = schedulers.zero, episodes=100, plot=False)
